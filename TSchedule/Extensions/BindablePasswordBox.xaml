<UserControl x:Class="TSchedule.Extensions.BindablePasswordBox"
             xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="PasswordControl">
    <Grid>
        <PasswordBox x:Name="passwordBox"
                     PasswordChanged="PasswordBox_PasswordChanged"
                     ui:PasswordBoxHelper.PasswordRevealMode="Peek"
                     Width="{Binding Width, ElementName=PasswordControl}"
                     IsEnabled="{Binding IsEnabled, ElementName=PasswordControl}"
                     ui:ControlHelper.Header="{Binding Header, ElementName=PasswordControl}"
                     ui:ControlHelper.PlaceholderText="{Binding PlaceholderText, ElementName=PasswordControl}" />
    </Grid>
</UserControl>
